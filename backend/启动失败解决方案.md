# 🐛 后端启动失败 - 快速诊断指南

## 错误信息
```
Failed to execute goal org.springframework.boot:spring-boot-maven-plugin:2.7.14:run
Application finished with exit code: 1
```

## 🔍 可能的原因

### 1️⃣ **数据库密码不正确** (最常见)

**检查方法**:
```bash
# 查看当前配置的密码
cd backend\src\main\resources
type application.properties | findstr password
```

**解决方法**:
```bash
# 方式1: 运行修复脚本
cd backend
修复数据库密码.bat

# 方式2: 手动修改
# 编辑 backend\src\main\resources\application.properties
# 将 spring.datasource.password=root 改为你的实际密码
```

### 2️⃣ **数据库未初始化**

**检查方法**:
```bash
mysql -u root -p
SHOW DATABASES LIKE 'student_grade_system';
```

**解决方法**:
```bash
cd backend
mysql -u root -p < init.sql
```

### 3️⃣ **MySQL服务未启动**

**检查方法**:
```bash
# Windows
services.msc
# 查找 MySQL 服务
```

**解决方法**:
```bash
# 启动MySQL服务
net start MySQL80
# 或者在服务管理器中启动
```

### 4️⃣ **端口4000被占用**

**检查方法**:
```bash
netstat -ano | findstr :4000
```

**解决方法**:
```bash
# 方式1: 杀死占用进程
taskkill /F /PID <进程ID>

# 方式2: 修改端口
# 编辑 backend\src\main\resources\application.properties
# 将 server.port=4000 改为其他端口
```

---

## ✅ 完整诊断流程

### 步骤 1: 运行诊断工具
```bash
cd backend
诊断工具.bat
```

### 步骤 2: 查看详细错误
```bash
cd backend
mvn spring-boot:run -X > startup-error.log 2>&1
# 然后查看 startup-error.log 文件
```

### 步骤 3: 逐一排查

#### ✓ 检查数据库连接
```bash
mysql -u root -p -e "SELECT VERSION();"
```

#### ✓ 检查数据库是否存在
```bash
mysql -u root -p -e "SHOW DATABASES LIKE 'student_grade_system';"
```

#### ✓ 检查表是否创建
```bash
mysql -u root -p student_grade_system -e "SHOW TABLES;"
```

#### ✓ 检查数据是否导入
```bash
mysql -u root -p student_grade_system -e "SELECT COUNT(*) FROM student;"
# 应该显示 300
```

---

## 🚀 推荐解决步骤

### 方案 A: 使用修复脚本 (最简单)
```bash
cd backend
修复数据库密码.bat
```
按提示输入你的MySQL密码，脚本会自动:
- 更新配置文件
- 测试数据库连接
- 给出下一步指示

### 方案 B: 手动修复

#### 1. 修改密码配置
编辑文件: `backend\src\main\resources\application.properties`

找到这一行:
```properties
spring.datasource.password=root
```

改为你的实际MySQL密码:
```properties
spring.datasource.password=你的密码
```

#### 2. 重新初始化数据库
```bash
cd backend
mysql -u root -p < init.sql
# 输入你的MySQL密码
```

#### 3. 清理并重新编译
```bash
cd backend
mvn clean package -DskipTests
```

#### 4. 启动后端
```bash
mvn spring-boot:run
```

---

## 📝 常见错误信息对照

### 错误1: Access denied for user 'root'@'localhost'
**原因**: 密码错误
**解决**: 修改 application.properties 中的密码

### 错误2: Unknown database 'student_grade_system'
**原因**: 数据库未创建
**解决**: 运行 `mysql -u root -p < init.sql`

### 错误3: Table 'student_grade_system.student' doesn't exist
**原因**: 数据库表未创建
**解决**: 重新运行 `mysql -u root -p < init.sql`

### 错误4: Communications link failure
**原因**: MySQL服务未启动
**解决**: 启动MySQL服务

### 错误5: Address already in use: bind
**原因**: 端口4000被占用
**解决**: 修改 server.port 或关闭占用进程

---

## 🆘 如果以上都无法解决

### 生成完整错误日志
```bash
cd backend
mvn clean spring-boot:run -X -e > full-error.log 2>&1
```

查看 `full-error.log` 文件，找到具体的错误堆栈信息。

### 关键信息位置
在日志中搜索:
- `Caused by:`
- `Exception:`
- `Error creating bean`
- `Failed to configure`

---

## 📞 快速检查清单

- [ ] MySQL服务已启动
- [ ] 数据库密码正确 (application.properties)
- [ ] student_grade_system 数据库已创建
- [ ] 表已创建 (300学生数据已导入)
- [ ] 端口4000未被占用
- [ ] Java版本 >= 17
- [ ] Maven可用

---

**记住**: 90%的启动失败都是因为**数据库密码配置错误**！
