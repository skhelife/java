# 🔧 页面打不开故障排查指南

## 快速诊断

### 方法1: 清除缓存重新加载
1. 按 `Ctrl + Shift + Delete` 打开清除浏览数据
2. 勾选"缓存的图片和文件"
3. 点击"清除数据"
4. 按 `Ctrl + F5` 强制刷新页面

### 方法2: 清除LocalStorage
1. 按 `F12` 打开开发者工具
2. 切换到 `Application` 或 `应用程序` 选项卡
3. 左侧选择 `Local Storage`
4. 右键点击你的网站 → 清除
5. 刷新页面

### 方法3: 使用无痕模式
1. 按 `Ctrl + Shift + N` (Chrome) 或 `Ctrl + Shift + P` (Firefox)
2. 打开 http://localhost:5173
3. 查看是否能正常显示

## 常见错误及解决方案

### 错误1: 白屏/页面空白

**可能原因**: JavaScript执行错误

**解决方案**:
1. 打开浏览器控制台 (F12)
2. 查看Console面板的红色错误信息
3. 截图错误信息反馈

**临时解决**: 在控制台执行
```javascript
localStorage.clear()
sessionStorage.clear()
location.reload()
```

### 错误2: 数据不显示

**可能原因**: Mock数据未初始化

**解决方案**: 在控制台执行
```javascript
// 强制重新初始化Mock数据
localStorage.removeItem('mockData')
location.reload()
```

**验证数据**: 
```javascript
// 检查数据是否存在
const mockData = localStorage.getItem('mockData')
if (mockData) {
    const data = JSON.parse(mockData)
    console.log('学生数:', data.students?.length)
    console.log('成绩数:', data.grades?.length)
    console.log('课程数:', data.courses?.length)
    console.log('教师数:', data.teachers?.length)
    console.log('班级数:', data.classes?.length)
}
```

### 错误3: Cannot read property 'xxx' of undefined

**可能原因**: 数据结构不匹配

**解决方案**:
```javascript
// 在控制台测试API
import { mockApi } from './services/mockApi'

// 测试统计数据
const stats = await mockApi.getStats()
console.log('统计数据:', stats.data)

// 测试班级数据
const classes = await mockApi.getClasses()
console.log('班级数据:', classes.data[0])

// 测试成绩数据
const grades = await mockApi.getGrades(1, 10)
console.log('成绩数据:', grades.data.content[0])
```

### 错误4: 模块导入失败

**错误信息**: `Failed to resolve module`

**解决方案**:
```bash
# 删除node_modules和锁文件
rm -rf node_modules package-lock.json

# 重新安装
npm install

# 重启开发服务器
npm run dev
```

### 错误5: 端口被占用

**错误信息**: `Port 5173 is already in use`

**解决方案**:
```bash
# Windows
netstat -ano | findstr :5173
taskkill /PID <进程ID> /F

# 或者修改端口
# 编辑 vite.config.ts
server: {
  port: 3000  # 改为其他端口
}
```

## 完整重置步骤

如果以上方法都不行，执行完整重置：

### 步骤1: 停止所有服务
关闭所有VS Code和终端窗口

### 步骤2: 清理前端
```bash
cd frontend
rm -rf node_modules
rm -rf dist
rm package-lock.json
```

### 步骤3: 重新安装
```bash
npm install
```

### 步骤4: 清除浏览器数据
1. 清除缓存
2. 清除LocalStorage
3. 清除Cookies

### 步骤5: 重新启动
```bash
npm run dev
```

## 数据验证脚本

### 在浏览器控制台运行以下脚本验证修复：

```javascript
// 验证统计数据
async function testStats() {
  const { mockApi } = await import('/src/services/mockApi.ts')
  const result = await mockApi.getStats()
  
  console.group('📊 统计数据验证')
  console.log('✅ studentCount:', result.data.studentCount)
  console.log('✅ teacherCount:', result.data.teacherCount)
  console.log('✅ courseCount:', result.data.courseCount)
  console.log('✅ classCount:', result.data.classCount)
  console.log('✅ gradeCount:', result.data.gradeCount)
  console.log('✅ avgScore:', result.data.avgScore)
  console.log('✅ passRate:', result.data.passRate)
  console.log('✅ excellentRate:', result.data.excellentRate)
  console.groupEnd()
  
  return result.data
}

// 验证班级数据
async function testClasses() {
  const { mockApi } = await import('/src/services/mockApi.ts')
  const result = await mockApi.getClasses()
  const firstClass = result.data[0]
  
  console.group('🏫 班级数据验证')
  console.log('✅ id:', firstClass.id)
  console.log('✅ classId:', firstClass.classId)
  console.log('✅ course:', firstClass.course)
  console.log('✅ teacher:', firstClass.teacher)
  console.log('✅ studentCount:', firstClass.studentCount)
  console.groupEnd()
  
  return firstClass
}

// 执行验证
console.log('🔍 开始数据验证...')
await testStats()
await testClasses()
console.log('✅ 验证完成！')
```

## 预期结果

### 正常的统计数据应该显示:
- 学生总数: 300
- 课程总数: 10
- 教师总数: 10+
- 教学班数: 5+
- 成绩总数: 900+
- 平均分: 70-85
- 及格率: 90-100%
- 优秀率: 10-30%

### 正常的班级数据应该包含:
```json
{
  "id": 1,
  "classId": "2023春-C001-01",
  "course": {
    "id": 1,
    "courseName": "高等数学A",
    ...
  },
  "teacher": {
    "id": 1,
    "name": "张教授",
    ...
  }
}
```

## 紧急联系

如果问题仍未解决，请提供：
1. 📸 浏览器控制台完整截图（包括错误信息）
2. 📸 Network面板截图（查看API请求）
3. 💾 LocalStorage数据导出（Application → Local Storage → 右键Export）
4. 📝 具体的操作步骤和现象描述

---

**更新时间**: 2025-12-09
**适用版本**: v2.1-hotfix
