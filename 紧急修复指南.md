# 🔥 紧急修复指南

## 当前状态
- ✅ 已启用强制数据重置模式
- ✅ 已添加详细调试日志
- ✅ 已创建数据清理工具

## 立即操作（按顺序）

### 方案A：最快速修复（推荐）

1. **运行修复脚本**
   ```
   双击运行: 💥紧急修复.bat
   ```

2. **打开清理页面**
   - 浏览器访问: `http://localhost:5174/clear.html`
   - 点击"第1步: 清空 LocalStorage"
   - 点击"第2步: 刷新页面"
   - 点击"第3步: 返回首页"

3. **登录并测试**
   - 账号: admin
   - 密码: 123456
   - 点击"班级分布"

### 方案B：手动排查

1. **完全关闭浏览器**（所有窗口）

2. **重新打开浏览器**

3. **访问** `http://localhost:5174`

4. **按 F12** 打开控制台

5. **在Console中执行**:
   ```javascript
   localStorage.clear()
   location.reload()
   ```

6. **等待页面加载完成**，查看Console输出

7. **查找关键信息**:
   ```
   📊 数据诊断报告
   - 学生总数: 300
   - 成绩总数: 应该 > 900
   - 班级数: 5
   
   👥 各班级学生统计:
   2023春-C001-01 (...): XX人  ← 如果这里是0就有问题
   ```

8. **如果还是0**，在Console执行:
   ```javascript
   const data = JSON.parse(localStorage.getItem('mockData'))
   console.log('成绩总数:', data.grades.length)
   console.log('第1个成绩:', data.grades[0])
   console.log('第1个班级:', data.classes[0])
   console.log('classId匹配测试:', data.grades[0].classId === data.classes[0].classId)
   ```

9. **截图Console输出**，发给我看

## 可能的问题

### 问题1: LocalStorage缓存旧数据
**症状**: 刷新后还是0
**解决**: 
- 方案A的清理页面
- 或者在Console执行 `localStorage.clear(); location.reload()`

### 问题2: classId类型不匹配  
**症状**: Console显示类型不同
**解决**: 需要看到具体的Console输出才能修复

### 问题3: 数据没有生成
**症状**: grades.length = 0
**解决**: 在Console执行 `window.mockDataTools.regenerate()`

## 调试信息收集

如果以上都不行，请在Console执行以下命令并截图：

```javascript
// 1. 检查数据完整性
const data = JSON.parse(localStorage.getItem('mockData'))
console.table({
    '学生数': data.students.length,
    '成绩数': data.grades.length,
    '班级数': data.classes.length,
    '课程数': data.courses.length,
    '教师数': data.teachers.length
})

// 2. 检查classId类型
console.log('班级classId:', data.classes.map(c => ({id: c.classId, type: typeof c.classId})))
console.log('成绩classId (前5条):', data.grades.slice(0,5).map(g => ({id: g.classId, type: typeof g.classId})))

// 3. 检查匹配
const cls = data.classes[0]
const gradesInClass = data.grades.filter(g => g.classId === cls.classId)
console.log(`班级 ${cls.classId} 的成绩数:`, gradesInClass.length)

// 4. 如果为0，尝试宽松匹配
if (gradesInClass.length === 0) {
    const looseMatch = data.grades.filter(g => String(g.classId) === String(cls.classId))
    console.log('宽松匹配结果:', looseMatch.length)
}
```

把所有输出截图发给我！
